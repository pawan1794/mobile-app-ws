spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/photo_app?useLegacyDatetimeCode=false&serverTimezone=UTC&autoReconnect=true&useSSL=false
    username: pawan
    password: pawan132
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /mobile-app-ws
  port: 5555

tokenSecret: jf9i4jgu83nfl0
userController.authorizationHeader.description: Bearer JWT Token